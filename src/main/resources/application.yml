spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog?serverTimezone=UTC&characterEncoding=utf8
    password:
    username:
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath*:/mapper/blog/*.xml
  type-aliases-package: me.perol.blog.entity
logging:
  level:
    root:
      info
